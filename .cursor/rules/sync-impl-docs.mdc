---
description: 实现行为变更时同步更新 docs/impl
alwaysApply: true
---

# 代码与 impl 保持同步

`docs/impl/` 描述**当前代码的实际行为**（索引见 `docs/impl/README.md`），不记录中间模型思考或变更过程。

改动实现行为（窗口、数据层、模块结构等）时，在同一任务内同步更新对应的 impl 文档，使文档与代码一致。纯样式、注释、格式化等不影响行为的改动可不改 impl。